/*
IMPLEMENT:
- an interface representing a single "item" in the cart
-- should have the following fields: id (number), name, stock/quantity (might be useful to make optional for now), equipment type (you might want to define a type for equipment type (i.e. stationary, electronic, etc.))

-- brainstorm functions that might be useful and try to implement them with sync functions and even async functions if ur comfortable. 
-- for instance: createItem(id), deleteItem(id), getItemById(id), listItems(p) - could list all items with a common parameter "p", p could be status, equipment type - just a generic placeholder for the example.
-- an "update" function will be useful but more complicated because we need a type for updates. updateItem(id, updates)

*/

